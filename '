const std = @import("std");

const Style = enum {
    BOLD,
    ITALIC,
    UNDERLINE
};

const character_cell = struct {
    char: u8,
    style:
}

const text_buffer = struct {
    width: u32,
    height: u32,
    cursorX: u32,
    cursorY: u32,
    bottomIndex: u32,
};
