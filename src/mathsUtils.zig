const std = @import("std");

const ivec2 = struct {
    x: i32,
    y: i32
};

const vec2 = struct {
    x: f32,
    y: f32
};

const vec4 = struct {
    x: f32,
    y: f32,
    z: f32,
    w: f32
};
